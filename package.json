{
  "name": "landing-page-cloudflare",
  "version": "1.0.0",
  "description": "Lightweight landing page deployed to Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
